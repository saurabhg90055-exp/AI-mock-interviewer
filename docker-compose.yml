version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_URL=sqlite:///./ai_interviewer.db
    volumes:
      - backend_data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  backend_data:

# For production with PostgreSQL, add:
# 
#   db:
#     image: postgres:15-alpine
#     environment:
#       POSTGRES_USER: aiinterviewer
#       POSTGRES_PASSWORD: ${DB_PASSWORD}
#       POSTGRES_DB: ai_interviewer
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     restart: unless-stopped
#
# And update backend DATABASE_URL to:
#   DATABASE_URL=postgresql://aiinterviewer:${DB_PASSWORD}@db:5432/ai_interviewer
